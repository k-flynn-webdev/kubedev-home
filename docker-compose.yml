version: "3.8"

services:
  kubedev-home:
    build:
      context: ./app
      dockerfile: ./Dockerfile
    ports:
      - "3001:3000"
    tmpfs:
      - /tmp
    volumes:
      - ./build:/app/build
